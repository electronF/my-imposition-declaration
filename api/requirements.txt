# ─────────────────────────────────────────────
# AxOvide Backend — Dépendances Python
# Python 3.11+
# Installation : pip install -r requirements.txt
# ─────────────────────────────────────────────

# ── Serveur web ──────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9        # multipart/form-data (upload fichiers)

# ── Validation et sérialisation ───────────────
pydantic==2.9.2
pydantic-settings==2.5.2       # lecture des .env
email-validator==2.2.0         # validation EmailStr dans Pydantic

# ── Base de données ───────────────────────────
sqlalchemy==2.0.35
aiosqlite==0.20.0              # SQLite asynchrone

# ── API Claude (Anthropic) ────────────────────
anthropic==0.34.2

# ── Extraction de texte PDF ───────────────────
pdfplumber==0.11.4             # extraction texte PDF natif
pypdf==4.3.1                   # fallback extraction PDF
python-docx==1.1.2             # extraction texte DOCX (CV)
Pillow==10.4.0                 # conversion PDF image → bytes pour Claude vision

# ── PDF image (scan) → Claude vision ─────────
pdf2image==1.17.0              # convertit pages PDF en images PNG/JPEG
# ⚠ Nécessite poppler installé sur le système :
#   Ubuntu/Debian : sudo apt-get install poppler-utils
#   macOS         : brew install poppler

# ── Utilitaires ───────────────────────────────
python-dotenv==1.0.1           # chargement .env
python-jose==3.3.0             # JWT (futur auth)
passlib==1.7.4                 # hachage (NAS → SHA-256)
httpx==0.27.2                  # client HTTP async (tests)

# ── Tests ─────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
